{
    "environment": {
	"type":       "${env_type}",
	"project":    "${env_project}",
	"url":        "${env_url}",
	"created_at": "${env_created_at}",
	"db": {
	    "name": "${db_name}",
	    "user": "${db_user}",
	    "pass": "${db_pass}",
	    "host": "${db_host}"
	}
    }
}